/**
 * Props per AdminHeader
 */
export default interface AdminHeaderProps {
  /** Pagina corrente per evidenziare il link attivo nella navigazione */
  currentPage?: 'dashboard' | 'guides' | 'categories';
}
